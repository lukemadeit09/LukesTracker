// Lukes Tracker — central theme.
// Minimalist, dark, disciplined. One place to tweak the whole look.

export const colors = {
  background: '#253237', // very dark base
  surface: '#2e3d44',    // slightly lifted panels (derived from background)
  secondary: '#4f5d75',  // slate — secondary surfaces / inactive
  muted: '#bfc0c0',      // silver — muted text + borders
  text: '#ffffff',       // main text
  accent: '#ef8354',     // orange — highlights, progress, streaks
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 20,
  pill: 999,
};

export const font = {
  // Sizes
  h1: 28,
  h2: 20,
  body: 15,
  small: 13,
  tiny: 11,
};

export default { colors, spacing, radius, font };
